# bard-infra — bug tracker

Per `shared-rules/process-rules.md §2`: each entry has a short description, date
observed, and a status beginning with exactly one of `Open`, `In Progress`, or
`Completed`.

## Open

No infra-native (`bard_infra/nameres`) bugs yet. The migrated fabric/platform
bugs (registry, router, agent, common, clients) are tracked in the
[Fabric / platform bugs](#fabric--platform-bugs-migrated-from-bard-llm) table
below; the open ones there are #56, #57, #59, #63, #66, #67.

## Notes

- 2026-06-15: Repo had no `pre-commit`/`gitleaks` installed at MVP scoping time
  (process gap, not a code bug). Tracked as plan item S0 in
  [PLANS.md](PLANS.md); not a defect in shipped infra.

## Fabric / platform bugs (migrated from bard-llm)

Bugs against the **fabric / platform** (registry, router, agent, common, the
Flutter `clients/app`, box/channel onboarding, the relay) migrated from
`bard-llm/bugs.md` when the fabric was re-homed into this repo (commit a9caafd,
2026-06-18). The iOS LlamaServer app bugs (`#1`–`#49` and the App-Store/StoreKit
bugs) stayed in bard-llm. **Original `#` numbers are preserved** — fabric bugs
and `features.md` entries cross-reference each other by these numbers (e.g. #54
broker-hijack, #56 relay auth, #58 JWT length, #60 registry scheme), so
renumbering would break the web of references. The `Status` column is normalised
to `Open` / `In Progress` / `Completed`; the original status text is kept after
the keyword. Pre-split history is frozen at tag
`archive/pre-infra-split-2026-06-18` in bard-llm.

| # | Description | Date | Status |
|---|---|---|---|
| 50 | **Pro: no way to run the models from the Flutter client.** The `clients/app/` client shows a Models list (sample data) and a Terminal, but there is no path to actually run inference: `_HomeShellState._api` is hardcoded `null` (`main.dart:39`) so the list never hits a live Registry, there is no Chat tab wired to the Router's `POST /v1/message` (`api.dart:35` `sendMessage` exists but is uncalled), and no Connections UI to point the client at a Router/Registry/Agent. Net: a user cannot select a model and get a completion. <br>Clarify: blocked on the app-structure rework (Dashboard / Connections / Terminal / Chat tabs) below — Chat tab + Connections config are the fix path. | 2026-06-08 | In Progress — partial 2026-06-08: client path built (`PLAN_client_tabs.md`): Connections tab configures a live Router/Registry/Agent + active selection; `AppState.api` replaces the hardcoded `null`; Chat tab posts to Router `/v1/message`; Models list fed by the Registry. Remaining for full close: verify end-to-end against a running backend, and persistence of the configured connection (shared_preferences follow-up). |
| 51 | **Pro: Sprint-1 agent Containerfile fails to build on UBI9 aarch64** — `podman build -f agent/Containerfile` errors at the runtime stage `RUN dnf -y install openssh-server openssh-clients libcurl`: `libcurl-minimal-7.76.1-40.el9.aarch64 ... conflicts with libcurl ... from ubi-9-baseos-rpms`. The `ubi9/python-311` base ships `libcurl-minimal`, which conflicts with the full `libcurl` that `llama-server` (built `-DLLAMA_CURL=ON`) needs. Cloud Claude marked Sprint 1 "code-complete" but never built the image (no podman in its sandbox), so this shipped unverified; surfaced on first real `podman build` on Eddie's machine (2026-06-09). | 2026-06-09 | Completed — fixed 2026-06-09 (v0.1.6): added `--allowerasing` to the runtime-stage dnf install (`agent/Containerfile`) so dnf swaps `libcurl-minimal`→`libcurl`. Verified end-to-end on arm64: `podman build` succeeds; container boots, llama-server loads the model + passes its health gate, agent `/healthz`=200, and `POST /infer` returns a real llama.cpp completion (`"Paris"`, not echo). |
| 53 | **Pro: two demo compromises left the fleet permissive** (Chris Wright demo, v0.8.0/v0.8.1). (a) `BARDPRO_REGISTRY_SCHEME=http` downgraded agent self-registration to cleartext with no friction — a single env var, no warning, no opt-in gate. (b) The agent container ran permissive: runtime stage stayed `USER 0` (root), `ssh-keygen -A` baked identical ssh host keys into every image, the entrypoint tried to start sshd in the MVP, `EXPOSE 22`, and the demo `podman run` commands carried no `--cap-drop` / `--read-only` / `--pids-limit` / `no-new-privileges`. Documented honestly in v0.8.1 ("MVP = permissive") but flagged for post-demo tightening. | 2026-06-10 | Completed — fixed 2026-06-10 (worktree branch `claude/laughing-bell-57o15u`, gated on demo close): (a) `load_config` now fails fast on `registry_scheme=http` unless `BARDPRO_ALLOW_INSECURE_HTTP=true`, logs a startup WARNING when opted in, and rejects unknown schemes; demo scripts carry the opt-in. (b) `agent/Containerfile` runs as non-root `bard`, drops `ssh-keygen -A`, exposes 8444 only; entrypoint no longer starts sshd (openssh-server stays installed for v2/ADR-0004); demo `podman run` adds `--cap-drop=all --security-opt=no-new-privileges --read-only --tmpfs /tmp --pids-limit=512`. Fine-grained SELinux per-task grants stay roadmap (features #48). |
| 54 | **Pro: broker link does not bind JWT `sub` to claimed agentId** — LokNet slice 1 (v1.1.0): an agent connects to the Router's `/v1/agent-link` WS and declares its `agentId` in the `hello` frame; the Router verifies the JWT is valid but does NOT check that the token's `sub` claim matches the claimed agentId. Any holder of a valid fleet JWT (one HMAC secret across the fleet today) can therefore register a link for *any* agentId and intercept its dispatched inferences — a hijack vector. Surfaced by the implementing agent 2026-06-10. Same single-trust-domain caveat as the rest of MVP auth, but worth closing. Confirmed by the independent security audit (H-1). | 2026-06-10 | Completed — fixed 2026-06-10 (v1.5.0): `handle_agent_link` now rejects the link (WS close 1008, not registered) when `claims["sub"] != agentId`. (Broader per-entity identity to retire the single shared secret remains H-2/v2.) |
| 55 | **Pro: JwtVerifier doesn't require exp/iss/sub — "forever token" hole** (security audit M-1, 2026-06-10). `common/auth.py` `jwt.decode` passes no `options`, so PyJWT only validates `exp` IF present — a token minted without `exp` never expires, defeating rotation. Shipped minter sets exp, but the verifier doesn't enforce it. Fix: `options={"require":["exp","iss","sub"],"verify_exp":True}`. Launch-gating before broker leaves owner's boxes. | 2026-06-10 | Completed — fixed 2026-06-10 (v1.5.0): `JwtVerifier.verify` now requires exp/iss/sub + verify_exp (+30s leeway). |
| 56 | **Pro: Maude relay /ws/voice has NO auth + broadcasts plaintext between sessions** (security audit C-1, CRITICAL-as-launch-gate, 2026-06-10). `claudeTalk/relay/.../transport_ws.py ws_voice` trusts `?session=` as identity (no token/bearer/origin check) and `PeerBroadcaster` fans every utterance to all other sessions; handles cleartext → no E2EE in the running system. CONTAINED today (binds 127.0.0.1/Tailnet-private). Hard gate: must NOT be exposed publicly under any "private/E2EE" branding until wss + account-bound bearer (FR-S1/S2) + real per-recipient E2EE (M0–M3) land. Add a guard refusing non-loopback bind without auth. **Update 2026-06-12 (B4, v1.5.4):** the Bard-fabric side is now enforced — Router relay (`/v1/message` + broker hello) verifies per-device tokens via `FleetOrDeviceVerifier` when `BARDPRO_DEVICE_IDENTITY_ENABLED` (unauthenticated/revoked/unknown/cross-device relay rejected, pentest-pinned; SECURITY_AUDIT C-1 downgraded). REMAINING: the claudeTalk `/ws/voice` transport itself must adopt per-device tokens (code outside the fabric). | 2026-06-10 | In Progress — fabric side closed 2026-06-12 (B4, v1.5.4); claudeTalk transport adoption remains the public-exposure launch-gate |
| 57 | **Plaintext secrets at repo roots — rotate ElevenLabs key** (security audit M-6, hygiene, 2026-06-10). `authkey.txt` (Tailscale auth key) + `claudeTalk/eleven.txt` (live-format ElevenLabs `sk_…`). **Gitignored + never committed (verified)** — leak-prevention worked — but two real creds sit readable on disk. Action: move to keychain/secret-manager + delete the files; **rotate the ElevenLabs key** (it's been readable on disk; precautionary per §6). <br>**2026-06-10:** `claudeTalk/eleven.txt` **deleted** (vestigial — ElevenLabs TTS removed 2026-05-27, no code reads it; on-disk copy gone). **Pending Eddie:** revoke the key in the ElevenLabs dashboard (elevenlabs.io → API Keys → revoke) — deleting the file doesn't invalidate the key. `authkey.txt` (Tailscale) left in place pending an in-use check (may be live demo infra) — handle separately. <br>**2026-06-10 (Eddie):** ElevenLabs key has a **hard, small spend cap** → abuse exposure is bounded; on-disk copy already deleted, so **rotation deprioritized / accepted risk** (Eddie not worried). ElevenLabs portion CLOSED. | 2026-06-10 | In Progress — partial: eleven.txt deleted + ElevenLabs accepted-risk (capped); **authkey.txt (Tailscale) still TBD** |
| 58 | **Pro: no minimum length enforced on `BARDPRO_JWT_SECRET`** (found by live pentest 2026-06-10). `common/config.py` requires the secret to be *present* but never validates its length; a short/weak shared secret is offline-brute-forceable (PyJWT itself warns `InsecureKeyLengthWarning` below 32 bytes for HMAC-SHA256, RFC 7518 §3.2). Compounds the single-shared-secret risk (audit H-2). Fix: reject `len(jwt_secret) < 32` in `_validate` (fail-fast like the other config checks). **Note:** must sweep test fixtures/demo scripts that mint with short secrets to ≥32 bytes so the suite stays green. | 2026-06-10 | Completed — fixed 2026-06-10 (v1.5.1): `Config.require()` enforces `MIN_JWT_SECRET_BYTES=32` (RFC 7518 §3.2) for `jwt_secret`; covers `from_config` + `agent/register`. Also landed `tests/test_security_pentest.py` — the live pentest as a CI regression suite (20 attack assertions incl. the #54 broker-hijack). |
| 59 | **Pro: no rate limiting on the fabric — DoS + online-brute-force surface** (security audit M-7/L-1 + pentest 2026-06-10). `/v1/message` and `/v1/agent-link` have no throttle, so (a) volumetric abuse can exhaust the single-instance Router, and (b) the shared JWT secret (#58/H-2) can be brute-forced online with no lockout. **Eddie's Q (2026-06-10): does exponential backoff thwart this?** No — *client-side* backoff is politeness an attacker ignores; DoS needs **server-side** enforcement. Fix (layered): (1) **app-level** per-IP+per-identity rate limit + **escalating lockout** on repeated auth failures (the consumer iOS app already does this — `LlamaServer/Server/RateLimiter.swift`: 10 fails/60s → 5-min lockout — port the pattern to the Python fabric); (2) **edge** for volumetric/distributed floods — Cloud Run `--concurrency`/max-instances caps (blast-radius, not a shield) + a CDN/WAF (Cloudflare) in front. App-level can't stop a botnet-scale flood; the edge must. <br>Clarify: in-proc limiter (ties Valkey #67 for multi-instance) vs rely on edge? thresholds? | 2026-06-10 | Open — launch-gate hardening (esp. before any public/hosted exposure) |
| 60 | **Pro: router→registry client scheme hardcoded `https`, ignores `BARDPRO_REGISTRY_SCHEME`/`BARDPRO_ALLOW_INSECURE_HTTP` — all-HTTP fleet can't dispatch** (found 2026-06-14 wiring the Android client to a local fleet). `router/main.py:21` builds `HttpRegistryClient(f"https://{registry_host}:{registry_port}")` with a literal `https://`. The documented HTTP opt-in (README §3: `BARDPRO_REGISTRY_SCHEME=http` + `BARDPRO_ALLOW_INSECURE_HTTP=true`) only governs the **agent's self-registration**, not the **router's own registry lookup**. Result: with a plain-HTTP registry, the router's `lookup()` (`clients.py:58`) dials `https://…/agents/{id}`, the TLS handshake fails against the HTTP server, `AgentUnavailable("registry unreachable")` is raised, and `/v1/message` returns **502 `agent_unavailable`** before the agent is ever contacted. Repro: native uvicorn fleet all-HTTP (registry :8071, router :8070, agent :8444 self-registered as `http://127.0.0.1:8444`) — agent `/infer` returns 200 echo when called directly, but router `/v1/message` → 502, and the agent log shows **no** `/infer` hit. Fix: use `f"{_config.registry_scheme}://…"` (defaults to `https`, so the full-TLS smoke_local path is unchanged), gated on the existing `ALLOW_INSECURE_HTTP` fail-fast that already guards the agent path; ship with a regression test asserting the router honors `registry_scheme=http`. Scope: blocks only the plain-HTTP local/dev fleet; full-TLS deployment unaffected. | 2026-06-14 | Completed 2026-06-14 — `HttpRegistryClient.from_config()` (router/clients.py) now builds the base URL from `registry_scheme` (defaults https; http gated by `allow_insecure_http`), and `router/main.py` uses it — mirrors the agent's existing pattern. Regression tests in `tests/test_clients.py` (https default + http opt-in); full suite 455 green @ 100% line+branch. Verified live: all-HTTP fleet `/v1/message` → 200 echo round-trip. |
| 61 | **Pro/Android: client can't reach a plain-HTTP local fleet — cleartext blocked by default + no network security config** (found 2026-06-15 finishing the Android↔fleet wiring after #60). The Flutter client targets `targetSdk 36`; Android blocks cleartext (plain HTTP) traffic by default for apps targeting API ≥ 28, and `AndroidManifest.xml` had no `networkSecurityConfig` / `usesCleartextTraffic`. Result: pointing the app at an all-HTTP fleet (the documented localhost default `http://127.0.0.1:8080`) fails with `java.io.IOException: Cleartext HTTP traffic to <host> not permitted` *before a socket opens* — surfaced in-app as a `BardApiException.network`. Compounded by `127.0.0.1` resolving to the device/emulator itself, not the host fleet. Scope: blocks only the plain-HTTP local/dev fleet on Android (API ≥ 28); HTTPS fleets and other platforms unaffected. This is the client-side mirror of #60 (server side hardcoded the wrong scheme; Android side forbade the right one). | 2026-06-15 | Completed 2026-06-15 — added `android/app/src/main/res/xml/network_security_config.xml`: `base-config cleartextTrafficPermitted="false"` (prod stays HTTPS-only, matching the #53/#60 HTTP-is-opt-in posture) + a `domain-config` permitting cleartext ONLY to loopback/emulator-alias hosts (`127.0.0.1`, `localhost`, `10.0.2.2`, `10.0.3.2`); referenced via `android:networkSecurityConfig` in the manifest. Client README gained a "Running against a local fleet from Android" section (`adb reverse` so the default `127.0.0.1` URL works on emulator+USB device, or `10.0.2.2` host alias). No Dart logic touched → Dart suite unaffected; `flutter analyze` clean, debug APK builds. Live emulator round-trip pending Eddie (CLAUDE.md §14 — can't drive the UI). |
| 62 | **Pro/client: `--dart-define` overrides silently ignored — `AppConfig` read `Platform.environment` only** (found 2026-06-15 on a real Android device: Dashboard showed "router/registry unreachable" despite a healthy fleet). `lib/config.dart` resolved `BARD_ROUTER_URL`/`BARD_REGISTRY_URL`/`BARD_AUTH_TOKEN` from `Platform.environment`, but Flutter `--dart-define` values are **compile-time constants** not exposed there — they are only readable via `String.fromEnvironment`. So every `--dart-define=BARD_ROUTER_URL=…`/`BARD_AUTH_TOKEN=…` was dropped and the app always fell back to `localhost:8080/8081` with an empty token. The client README documented `--dart-define` as the override mechanism, so this bit any non-default backend (incl. the Android↔fleet wiring). On desktop a real exported env var still worked (Platform.environment), masking the bug. | 2026-06-15 | Completed 2026-06-15 — `AppConfig` now reads each key with `String.fromEnvironment` (consts `_routerDefine`/`_registryDefine`/`_tokenDefine`) and resolves via a new public `AppConfig.resolve(envValue, define, fallback)` helper (precedence: non-blank runtime env → non-blank dart-define → default). Added `config_test.dart` "resolve precedence" group covering all branches; `flutter analyze` clean, 75 Dart tests pass. Verified on-device: app reached the fleet and the Models list populated. |
| 63 | **Pro: invite-onboarded (per-device) token rejected at Registry `/agents/{id}` lookup — a redeemed device can't reach an agent on the data path** (found 2026-06-15 in a live e2e test of the channel-invite onboarding spine). The Router wraps its endpoints in `FleetOrDeviceVerifier`, so a per-device token (minted from the one-time `deviceSecret` returned at redeem) IS accepted at `POST /v1/message`. But the Router's registry lookup (`HttpRegistryClient.lookup`, `router/clients.py:74`) re-presents that **same caller token** to `GET /agents/{id}`, and the Registry gates its endpoints with the **fleet** `JwtVerifier` only (it does not wrap them in `FleetOrDeviceVerifier`). So an invite-onboarded device authenticates at the Router but fails the registry-lookup hop → 401 → surfaced as **502 `agent_unavailable`**. Onboarding itself (create→redeem→active→channel membership, all PASS) is unaffected; this blocks only the *data path* for a device admitted purely by invite. Repro in the e2e: device token → 502 (auth accepted, downstream fail) vs wrong-secret token → 401 (auth rejected). Fix options: (a) Registry accepts per-device tokens on its read endpoints (wrap in `FleetOrDeviceVerifier`); or (b) Router uses a fleet/service token for its own internal lookups instead of forwarding the caller's token. | 2026-06-15 | In Progress — fixed in code, pending review. Took option (b) (smallest blast radius, Registry stays fleet-only): new `FleetTokenMinter` (`common/auth.py`) signs a short-lived fleet token from the Router's own `BARDPRO_JWT_SECRET`; `create_app` takes a `service_tokens` minter and uses it for the internal `/agents/{id}` lookup hop instead of forwarding the caller's per-device token (the caller's identity still rides to the agent on the infer hop). Wired in `router/main.py`. Regression in `tests/test_router.py`: per-device caller now 200 with the minter vs the pre-fix 502 without it, plus fleet-caller-unaffected and `FleetTokenMinter` unit tests. |
| 66 | **Pro/client (box onboarding): UI extremely sluggish — taps & screen transitions lag on the iPhone box-onboarding build.** Observed 2026-06-18 tapping through the Flutter `clients/app` box-onboarding build on a real iPhone (iOS 26.5). Clicks and transitions are very slow. **Most-likely cause: it's a wireless _debug_ build** — `flutter run --debug` over wireless on iOS 26 (Flutter itself prints "Wireless debugging on iOS 26 may be slower than expected"); debug = JIT + asserts + VM-service attached over the network, all of which tax responsiveness independent of app code. Secondary (app-level) suspects: box/members polling or other network calls on the main isolate; per-frame rebuilds in the box screen. **Triage before chasing code:** re-test a `--release` build (or a wired USB run) to isolate the debug/wireless artifact from a genuine perf bug; only if release is also sluggish, profile the box screen. Distinct from the Swift LlamaServer typing lag (bard-llm bug #48 — different app/codebase). | 2026-06-18 | Open |
| 67 | **Pro/client (box onboarding): "unauthorized" creating a box — the baked auth token had EXPIRED** (found 2026-06-18 on the iPhone box-onboarding build; "Create a box" → unauthorized). **Root cause is NOT the box-flow code — it's an expired credential.** The Mac+iPhone builds were compiled with `--dart-define=BARD_AUTH_TOKEN=<the fabric's /tmp/bardpro-fabric/manager.jwt>`, a **24-hour fleet token** (`sub=fabric-manager`, `iss=bardllm-pro`, `exp=1781716930`) minted when the fabric came up the prior day; by test time it had expired ~19h earlier (now 1781785381 > exp). Confirmed server-side: `POST /invites` with that token → **401 `{"error":"unauthorized","retry":false}`**, identical to no-token and bogus-token, because #55's `JwtVerifier` correctly enforces `verify_exp`. Immediate unblock: re-mint a fresh fabric token + rebuild the clients. **Durable fix (the real one):** a client must NOT bake a short-lived fleet token at build time. The correct owner identity for create-box is either a longer-lived/renewable **owner credential** or an **enroll step** that yields a per-device token (the same per-device path redeemers use from `deviceSecret`) before create-box — the fleet/manager token is a dev convenience only. Track the durable fix here. | 2026-06-18 | Completed 2026-06-18 (S5, branch `feat/s5-flutter-owner`) — durable fix landed: the client now uses ONE per-device Ed25519 identity (ADR-0016 §1) for create-box. `BoxController.createBox` self-registers the device (`POST /devices/self-register`), then signs `POST /channels` + `POST /invites` with the device's OWN self-signed EdDSA token (`DeviceIdentity.mintTokenFor` → `BardApi.tokenProvider`), NEVER `BARD_AUTH_TOKEN`. Owner management (members/remove/add-via-share) uses the same device token. The baked fleet token no longer authorizes any owner action — `BARD_AUTH_TOKEN` is not required for box ownership. New `lib/box/device_identity.dart`; tests assert the bearer is a device-signed EdDSA JWT and not the baked token (170 Flutter tests, analyze clean). |
